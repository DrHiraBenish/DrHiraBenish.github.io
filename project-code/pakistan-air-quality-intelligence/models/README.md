# Generated models

`src/train_model.py` writes `lahore_pm25_enhanced_change_model.joblib` here. Model binaries are excluded from Git because they should be regenerated from the documented data and code.

